[project]
name = "perfetto-tools-local"
version = "0.1.0"
requires-python = ">=3.10,<3.15"
dependencies = [
  "perfetto==0.57.2",
  "protobuf==7.35.1",
]

[dependency-groups]
dev = [
  "pytest==9.1.1",
]

[tool.uv]
package = false
required-version = "==0.11.28"
