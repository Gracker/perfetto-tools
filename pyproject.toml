[project]
name = "perfetto-tools-local"
version = "0.1.0"
requires-python = ">=3.10,<3.15"
dependencies = [
  "perfetto==0.57.2",
  "protobuf==6.33.6",
]

[dependency-groups]
dev = [
  "pytest==8.4.2",
]

[tool.uv]
package = false
required-version = "==0.11.28"
